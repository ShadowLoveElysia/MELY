# MELY R1 集成需求与实施审批基线

## 0. 文档状态

本文档统一整合 MELY 已提出的产品需求、当前工作区实现、已取得的验证证据、尚未闭环的风险，以及批准后的实施顺序。

- 当前状态：**用户已于 2026-08-09 批准 MELY R1 整合基线，正在按照第 9 节实施与验收**。
- 实施阶段：以第 8 节为发布阻断清单，不以“代码已经存在”或旧报告代替最终源码的真实端到端证据。
- Windows Build Tools、Windows SDK、目标游戏或模组环境仍需在对应阶段单独取得用户授权或由用户提供。
- 项目名称与目录：`MELY`，工作目录 `/mnt/h/小说/MELY`。
- 品牌含义：`M` 代表 Minecraft，`ELY` 代表 ELYSIA。
- 本文所称“完成”必须同时满足实现、自动化证据、真实模型证据和适用的目标端 smoke。
- 标准真实负载：`H:\Downloads\爱莉希雅—霁月婵娟2.0_by_神帝宇_ed5668c5d5c3b3063039ec8a4e83f102.zip`；不得用轻量 fixture 替代最终性能与视觉验收。
- 已归档视觉参考：`docs/references/ui-workbench-reference.png` 与 `docs/references/current-face-readability-reference.png`。
- 此前任务在收到“先整合、后批准”指令前已经写入产品代码、测试与审计入口，其中还包含一组尚未验证的内存释放草稿。本稿如实记录其现状，但这些改动不代表用户已经批准设计，也不代表它们已经达到可发布质量。
- 本次批准只代表认可 R1 的产品范围、冻结决策、验收门槛与执行顺序；不等于批准当前内存草稿、最终面部档位、系统级安装或 R1 正式发布。

状态定义：

- `VERIFIED`：实现存在，且某一已记录源码基线已有可靠自动化或真实模型证据；最新源码发生变化后仍须按第 8 节重跑。
- `IMPLEMENTED`：主体实现存在，但仍需真实 E2E、视觉审批或目标端验证。
- `DRAFT`：工作区已有未完成或未验证的实现草稿，不能作为批准或发布依据。
- `PENDING`：R1 必须完成，当前仍有明确缺口或失败证据。
- `EXTERNAL`：代码侧已具备基础，仍依赖 Minecraft、Windows 工具链或安装环境验证。

## 1. 产品定位

MELY 是一个本地运行的 Web 与 Windows 桌面双运行时工具，用于把 MMD 角色模型与当前姿势转换为 Minecraft 雕像工程。

输入：

- PMX、PMD、模型 ZIP、模型文件夹和配套贴图。
- VMD 动作文件。
- MELY Pose JSON。

输出与施工工具：

- Java 版 `.litematic`。
- Sponge Schematic v3 `.schem`。
- Bedrock `.mcstructure`。
- Bedrock `.mcfunction` 行为包 ZIP。
- 包含总体投影、`32 x 32 x 32` 子投影、拼接坐标、材料和箱子计划的完整工程 ZIP；默认只打包 Litematica 工程，跨平台格式由用户显式勾选。
- 浏览器内材料清单、箱子规划和 X/Y/Z 三轴分层建造指南。

R1 提供两条生成路线：

1. 实体体素模式：强调当前姿势、贴图颜色、Alpha 过滤、薄面保留、皮肤保护和面部可读性。
2. 灵动虚空全息模式：仅稀疏勾勒表面、轮廓、脊线和面部特征，使用竖直末地烛与独立竖直白色玻璃板。

导入成功后首先展示真实 MMD 3D 模型。Minecraft 投影只在用户主动生成后出现，不以演示模型替代真实导入结果。

## 2. 冻结产品决策

| 项目 | MELY R1 决策 |
| --- | --- |
| Java 目标版本 | Minecraft Java 1.20.1 |
| Java DataVersion | `3465` |
| Litematica | Version 6，SubVersion 1 |
| Bedrock 目标版本 | Minecraft Bedrock 1.20.10 |
| 版本兼容策略 | R1 只承诺上述两个目标版本；以后向下兼容必须通过版本档案、调色板降级和不支持方块替换，不能只改 DataVersion |
| 默认角色高度 | 320 格 |
| 原版安全范围 | `-64..319`，总跨度 384 格 |
| 超限高度预设 | 固定最大 2032 格；R1 不开放任意世界高度档案编辑器 |
| 自动分片 | 固定 `32 x 32 x 32` |
| 子投影命名 | 使用稳定坐标编号；头部/身体/腿部语义命名不进入 R1 |
| 工程 ZIP 默认内容 | 总体与子区 Litematica、拼接 manifest、材料/箱子/坐标指南；Schematic、mcstructure、mcfunction 默认不勾选 |
| 末地烛 | 只允许竖直，Java 状态固定 `facing=up` |
| 白色玻璃板 | 四向连接状态均为 `false`，且坐标阶段消除水平可连接邻居 |
| 默认生成模式 | 全息模式；导入后仍先显示原 MMD 模型 |
| 默认实体结构 | shell 表面壳；filled 仅在资源预检允许时运行 |
| 默认面部增强 | `balanced` |
| 默认抖动 | 0%，保持赛璐璐式干净色块 |
| 默认保护 | 皮肤保护、重力方块过滤、稀有方块过滤开启 |
| 默认发光映射 | 开启，可由用户调整材质选择 |
| 箱子规划 | 默认按类别分箱；同类别可紧凑分配，不同类别和辅助方块不混箱 |
| 分层指南 | R1 为网页交互与本地进度；图片/PDF 导出及跨设备同步延后 |
| 国际化 | 简体中文、英语、日语，使用标准键值 I18N |
| 内存硬门槛 | 代表性流程完整进程树峰值低于 2 GiB；危险任务必须在计算前拒绝 |
| 桌面技术 | Tauri v2 |
| Windows 入口 | BAT 优先启动打包 EXE；无 EXE 时回退本地 Web |
| 发布 smoke | Litematica、WorldEdit、Bedrock 和 Windows 安装包 smoke 均为 R1 发布硬门槛 |

后续提出的“所有末地烛必须竖直”覆盖早期“按法线自适应方向”的设想。R1 不生成横向末地烛或横向玻璃棍。

产品范围批准不等于系统级安装授权。安装 Visual Studio Build Tools、Windows SDK 等依赖时，必须再单独征得用户允许。

## 3. 核心用户流程

1. 导入 PMX、PMD、ZIP 或模型文件夹及贴图。
2. 在 Three.js 视口中检查真实 MMD 模型。
3. 可选导入 VMD，播放、暂停、空格切换、拖动时间轴或逐帧定位。
4. 可选进入手动姿势编辑，选择骨骼并使用 TransformControls 调整，支持 IK 约束链。
5. 锁定当前动作。存在 VMD 时，未锁帧不得生成投影。
6. 在主线程求值当前骨骼与 Morph，生成 CPU 变形后的最小网格快照。
7. 使用 Transferable TypedArray 将快照交给单一 Web Worker。
8. Worker 执行全息或实体体素化，并持续报告阶段进度。
9. 用户切换源模型、实体投影、全息投影、面部聚焦和日夜预览。
10. 用户调整主题、发光映射、抖动、皮肤保护和面部增强。
11. 用户打开材料、箱子和三轴分层施工工具。
12. 用户从统一导出中心选择五类出口之一。
13. 当配置高度或实际投影跨度超过 384 时，所有出口必须经过三道安全门。

## 4. 系统架构

### 4.1 React 组件边界

    App
    ├─ Sidebar
    │  ├─ 模型、贴图与动作导入
    │  ├─ Motion & Pose
    │  ├─ 骨骼编辑与 IK
    │  ├─ 尺度与高度安全
    │  ├─ 实体体素参数
    │  └─ 全息参数
    ├─ Viewport3D
    │  ├─ MMD 源模型渲染
    │  ├─ 实体/全息投影渲染
    │  ├─ TransformControls
    │  ├─ 透视/正交相机
    │  └─ 面部聚焦、网格、包围盒与夜景
    ├─ SurvivalTools
    │  ├─ 材料清单
    │  ├─ 箱子规划
    │  └─ X/Y/Z 分层指南
    └─ Windows
       ├─ 超限高度解锁
       ├─ 导出中心
       └─ 超限导出二次确认

R1 不为目录形式大规模重写现有 `App`。只有在能降低真实复杂度或显著改善测试边界时，才迁移为 feature hook 或子组件。

UI 视觉契约：

- 参考 `docs/references/ui-workbench-reference.png` 的工作台结构：左侧密集、可滚动的参数区，大面积无遮挡 3D 视口，顶部或视口边缘使用紧凑图标工具栏。
- 第一屏必须是可操作的模型与投影工作区，不增加营销 Hero 或装饰性大卡片。
- 视口始终是主视觉；参数面板不得挤压到无法检查 320 格角色全身和面部聚焦。
- 深色中性背景配合青色状态强调，但不机械复制参考软件的品牌、排版或控件细节。
- 桌面、紧凑桌面和移动布局维持同一操作层级，所有仅图标按钮提供本地化 tooltip 和 aria-label。

### 4.2 核心数据层

- `LoadedMmdModel`：Three.js 模型、动画、骨骼、IK、Morph、贴图和释放逻辑。
- `MmdMeshSnapshot`：当前帧 CPU 变形坐标、三角形、UV、材质、纹理像素和 faceFrame。
- `ProjectionDocument`：版本化、稀疏、与渲染器无关的统一投影文档。
- `ProjectionDocument` 是材料统计、箱子计划、分层指南、自动分片和全部导出格式的唯一事实来源。
- 每个内部方块记录坐标、规范方块 ID、版本化 state，以及材质、发光和来源标签；算法层不得混入 Java NBT 或 Bedrock palette 字段。
- `GameVersionProfile` 保存目标版本、DataVersion、可用方块、状态映射、重力/稀有过滤和皮肤白名单。
- Litematica、Schematic、mcstructure 和 mcfunction 是相互独立的编码适配器，只消费 `ProjectionDocument` 与目标 `GameVersionProfile`。
- 方块注册表同时保存规范 ID、Java 1.20.1 状态和 Bedrock 1.20.10 映射；未知版本、状态或不可降级方块必须明确拒绝。

### 4.3 Worker 通信

主线程发送：

- `GENERATE_HOLOGRAM`：`MmdMeshSnapshot`、`HologramOptions`、`jobId`。
- `GENERATE_SOLID`：`MmdMeshSnapshot`、`SolidOptions`、`jobId`。
- 大型 TypedArray 使用 Transferable Objects，不复制顶点、索引或纹理像素。

Worker 返回：

- `PROGRESS`：`jobId`、稳定阶段码、`0..1` 进度。
- `RESULT`：稀疏坐标、方块/材质索引、统计、边界和 faceFrame。
- `ERROR`：稳定错误码与 I18N 参数，不返回硬编码用户文案。

取消、替换或重新生成时必须终止旧 Worker、忽略迟到消息，并清空输入与结果引用。

### 4.4 计算边界

- 主线程：Three.js 场景、VMD 求值、骨骼/IK/Morph 状态和 CPU 网格快照。
- Worker：Triangle-AABB、Alpha/UV 采样、CIEDE2000、皮肤保护、主题、抖动、面部投射和最终稀疏体素。
- CPU 快照分批让出事件循环，不保留不必要的第二份完整网格或贴图副本。
- shell 和全息不得创建完整 dense 三维数组。
- filled 仅在体积和内存预检通过后进入三角形遍历。

## 5. 当前功能状态

| 功能域 | 状态 | 当前证据与剩余验收 |
| --- | --- | --- |
| PMX/ZIP/贴图导入 | VERIFIED | 爱莉希雅 ZIP 可加载并渲染；错误贴图路径有告警机制 |
| PMD、文件夹、多模型切换 | VERIFIED | `mmd-input-audit` 已验证相对路径、双 PMD、缺贴图告警、模型切换与旧模型 dispose；第三方复杂 PMD 仍是兼容性风险 |
| 原始 MMD 3D 预览 | VERIFIED | 真实模型、透视/正交、网格、包围盒和响应式视口可用 |
| 手动 K 姿势与 IK | VERIFIED/RESIDUAL | 真实 PMX 的手动骨骼选择、变换、撤销/重做，以及 fixture IK 已有 E2E；仍缺真实非标准 IK 资产 |
| VMD 时间轴 | VERIFIED/RESIDUAL | 持久化浏览器报告已覆盖播放、点击/空格暂停、拖帧、逐帧、边界钳制和锁帧；用户 ZIP 本身不含 VMD，当前使用仓库 fixture 与真实 PMX 联合验证 |
| 锁帧生成 | VERIFIED | 锁帧前禁生成、F0/F30 源画面与投影差异均通过 E2E |
| Pose JSON | VERIFIED | 当前姿势导出、回导哈希一致与缺失骨骼处理均已有持久化证据 |
| CPU Skinning | VERIFIED | 真实爱莉希雅 PMX 抽样 4,096 个 BDEF 顶点，最大误差 `9.555e-7`；SDEF/QDEF 由独立数学 fixture 覆盖，真实模型本身不含这两类顶点 |
| Worker 与模型生命周期 | DRAFT/PENDING | 旧基线的取消、替换、迟到事件与 dispose 单测通过，十轮功能循环完成且采样健康；工作区现有新的纹理、loader 缓存、模型卸载和 render-list 释放草稿，但尚未通过 TypeScript、定向测试或十轮实测，且静态复核已发现闭包仍可能保留旧 mesh、render-list 清理触发范围过宽两项待修问题 |
| 2 GiB 内存 | PENDING | 十轮真实审计峰值 `2,264,969,216` bytes，超出 2 GiB `117,485,568` bytes；30 秒恢复比 `1.204318` 也略高于 `1.2`，峰值主要来自 renderer 进程 |
| 实体防错体素化 | VERIFIED | 真实 320 shell、Alpha、UV、Triangle-AABB、皮肤保护、主题、发光与抖动已有自动化和截图；最终面部审美仍单独审批 |
| 面部增强三档 | IMPLEMENTED/PENDING | 可见表层投射和结构审计通过；已有九张等机位图和审批总览，但最终档位与默认档仍需用户明确批准 |
| 面部一键聚焦 | VERIFIED | 真实模型的透视/正交聚焦已通过 E2E；三档等机位审美对比仍需用户批准 |
| 全息稀疏线框 | VERIFIED/EXTERNAL | 真实 320 高生成成功，竖直末地烛与独立白色玻璃板有状态测试；仍需目标游戏邻居更新 smoke 与用户视觉批准 |
| 夜景与发光映射 | VERIFIED | 真实模型同坐标开关对比与日夜截图通过；夜景仅声明为艺术化预览 |
| 四种材质主题 | VERIFIED | 真实模型 `original`、`greekMarble`、`steampunk`、`ancientRuins` 对比与调色板约束通过 |
| 0..100% 抖动 | VERIFIED | 真实模型 0/100 对比保持坐标、改变状态哈希，皮肤保护断言通过 |
| 材料清单 | VERIFIED | 真实投影的数量、潜影盒/组/散件换算及辅助块计入均通过 UI E2E |
| 箱子规划 | VERIFIED | 类别变化强制换箱、辅助块独立、槽位与数量守恒已在真实投影上通过 |
| X/Y/Z 分层指南 | VERIFIED | 三轴全部占用层、图例、块数、进度持久化及 390x844 触控切层均通过 |
| `32^3` 自动分片 | VERIFIED | 子块无重叠，三轴不超 32，并集可还原源投影 |
| Litematica 1.20.1 | IMPLEMENTED/EXTERNAL | NBT 回读通过；缺 Litematica 模组实机导入 |
| Sponge Schematic v3 | IMPLEMENTED/EXTERNAL | gzip NBT/VarInt 回读通过；缺 WorldEdit 实机导入 |
| Bedrock mcstructure | IMPLEMENTED/EXTERNAL | little-endian NBT 回读通过；缺 Bedrock 1.20.10 结构方块实机导入 |
| Bedrock mcfunction | VERIFIED/EXTERNAL | App 使用流式行为包 ZIP，自动化通过；缺 Bedrock 行为包实机执行 |
| 完整工程 ZIP | VERIFIED | 145,189 方块真实实体投影的总体与 144 个子区可回读，坐标/状态守恒，施工顺序与内容哈希有效，默认不含可选跨平台格式 |
| 2032 稀疏工程 | VERIFIED/EXTERNAL | 真实爱莉希雅 2032 全息工程 ZIP 已完成：113,936 方块、4,234 个分片，坐标/状态/哈希/施工顺序守恒，完整进程树峰值约 1.33 GiB；仍缺高度扩展世界实机 smoke |
| 三道超限安全门 | VERIFIED | 384、目标 385、目标 320 但实际跨度 385、窄小 2032 与真实比例 2032 均通过统一安全判定；五出口共 18 次下载无绕过 |
| 标准键值 I18N | IMPLEMENTED/PENDING | 中英日键、插值契约、TSX UI 文案及玩家指南测试通过；但核心与平台层仍有英文 `Error`/`RangeError` 可能经 `error.external` 原样进入中文或日文 toast，需改为稳定错误码或证明仅为不可达 invariant，并补三语错误路径 E2E |
| BAT 快速启动 | IMPLEMENTED/EXTERNAL | EXE 优先/Web 回退契约存在；缺 Windows 双击 smoke |
| Tauri v2 打包 | EXTERNAL | 配置、图标和原生保存接口存在；当前机器缺 `link.exe` 与 Windows SDK |
| 最终源码发布回归 | PENDING | 最新资源释放草稿写入后尚无权威的 TypeScript、全量测试、Vite、真实模型 E2E、格式守恒、320/2032 工程包与生命周期报告；旧报告只能作为历史基线 |

## 6. 核心产品与算法契约

### 6.1 VMD、锁帧与 Pose

- VMD 时间轴显示帧号，帧率来自动作信息。
- 拖动时间轴时自动暂停并立即更新 mixer 时间。
- 空格仅在焦点不处于输入、按钮、选择框或弹窗时控制播放/暂停。
- 逐帧按钮在 `0` 与 `maxFrame` 钳制。
- 点击锁定后冻结当前帧，时间轴和播放入口进入锁定状态。
- 存在 VMD 时，未锁帧不得生成；静态模型或 Pose JSON 不额外要求锁帧。
- Pose JSON 1.0 保存相对 rest pose 的非默认骨骼与非零 Morph。
- 导入按日文名和英文别名匹配；缺失项报告但不崩溃；未包含 Morph 清零。

### 6.2 CPU Skinning

当前帧顶点求值顺序固定为：

1. 应用当前顶点 Morph。
2. 按 PMX 变形类型执行 BDEF1、BDEF2、BDEF4、SDEF 或 QDEF。
3. 应用 bindMatrix、骨骼矩阵和 bindMatrixInverse。
4. 应用模型根节点世界矩阵。
5. 输出体素化所需的最小 Float32 坐标及共享索引/材质数据。

真实验收规则：

- 从真实模型随机抽样不少于 1000 个线性 BDEF1/2/4 顶点。
- 同时以 `THREE.SkinnedMesh.getVertexPosition()` 和独立显式 BDEF 公式作为参考，统一转换到模型根节点世界坐标。
- 报告样本数、BDEF1/2/4 数量、最大误差、RMS、P95 和姿势前后发生变化的顶点数。
- 线性 BDEF 样本的最大世界坐标误差必须不高于 `1e-4`。
- Three.js 的 `getVertexPosition()` 是线性蒙皮参考，不能作为 SDEF/QDEF 的数值真值；SDEF/QDEF 以独立数学单测、真实模型类型计数和姿势变化证据验收，不混入 BDEF 误差统计。
- Morph 必须单独证明执行顺序为“先 Morph、后蒙皮”，并覆盖稀疏 morph split。

### 6.3 实体面部增强

面部增强只重着色已有最前表面方块，不新增坐标、不向外鼓脸、不改变整体轮廓。

实现契约：

1. 使用 faceFrame 的 right、up、forward 建立面部局部坐标。
2. 按局部横向和纵向建立 faceBase 单元。
3. 每个单元沿 forward 选择最前可见皮肤表面。
4. 从材质语义识别 eye、brow、mouth 和 overlay 源体素。
5. 将深层特征投射到对应最前单元。
6. `balanced` 与 `strong` 只在面部切平面扩张；左右眼和眉不能跨越中线连片。
7. 头发优先级与面部区域边界阻止颜色污染刘海、耳饰或皮肤外区域。
8. `off`、`balanced`、`strong` 的方块坐标集合必须完全一致。

当前结构证据：

- 三档均为 145,189 方块，尺寸 `249 x 318 x 208`，Regions 144。
- `balanced` 在最前脸层改变 128 个单元。
- `strong` 在最前脸层改变 154 个单元。
- `strong >= balanced`，三档坐标完全一致，眼眉跨中线检测为零。
- 结构审计不能替代审美判断；默认档最终由用户看图批准。

视觉验收以 `docs/references/current-face-readability-reference.png` 暴露的问题为基线：全身结构和色彩已可辨认，但在默认整身观看距离下，眼睛、眉毛和嘴部不能只依赖极少数深色像素。最终截图必须同时提供整身、正脸近景和斜侧近景；特征需在整身视图中仍可读，近景不得出现双眼跨中线、黑色连片、脸型外扩或发饰污染。

### 6.4 全息线框

- 不填充内部，只采样表面、轮廓、脊线、衣饰折线与重要面部区域。
- `sampleSpacing` 控制稀疏度；面部使用独立保留密度。
- 末地烛全部写为 `facing=up`。
- 白色玻璃板写入 `north=false`、`south=false`、`east=false`、`west=false`。
- 坐标阶段消除玻璃板四个水平方向的可连接邻居。
- mixed 模式可混用两类方块，但全部保持竖直。
- 正常生成不得包含隐藏占位或支撑块。

### 6.5 生存工程工具

材料换算：

- 每种方块输出总数、潜影盒、整组和散件。
- 一个潜影盒为 27 槽，每槽按该方块真实 `stackSize`。
- `5120` 个 64 堆叠方块的正确结果为 `2 潜影盒 + 26 组`，原需求示例中的 `1 盒 + 15 组 + 32` 不成立。
- 加入辅助垫脚方块后，总量必须等于主体投影加辅助块。

箱子规划：

- 一个大箱子为 54 槽，一个槽不能分配给两种材料。
- 默认按 `主体结构 / 照明 / 玻璃 / 装饰 / 辅助` 分类。
- 不同类别不混箱；同类别可紧凑排布，辅助方块单独规划。
- 跨箱拆分时数量守恒，输出箱号、槽位范围、材料、数量和剩余槽位。
- R1 不增加“紧凑混装/单材料专箱”策略选择器。

分层指南：

- 支持 X、Y、Z 三轴切片。
- 只索引有方块的层；上一层/下一层跳到相邻占用层。
- 画布显示坐标、方块色、图例、层块数、缩放和平移。
- 完成进度按投影指纹保存在本地；投影变化后不得串用旧进度。
- 超大稀疏坐标不得创建完整二维巨型矩阵。

### 6.6 艺术增强

- 夜景是艺术化预览，不宣称精确模拟 Minecraft 光照传播。
- 发光来源包括 PMX 信息、受控名称语义和用户手动选择。
- 关闭发光映射不得改变体素坐标。
- 主题包括 `original`、`greekMarble`、`steampunk` 和 `ancientRuins`。
- 主题原则上只改变方块选择；遗迹主题可在暴露空气面添加受控装饰，但不能替换发光源。
- 抖动强度为 `0..100%`；0% 为平色，100% 为完整 Floyd-Steinberg 误差扩散。
- 皮肤与面部保护区域不得接收制造噪点的抖动结果。

### 6.7 导出格式

Litematica：

- Java 1.20.1、DataVersion 3465、Version 6、SubVersion 1。
- Palette 含 air，BlockStates 按 Litematica 位打包规则写入。
- 使用最多 32 立方的 Regions，支持稀疏超高坐标。

Sponge Schematic：

- Sponge v3、gzip NBT、VarInt BlockData。
- 单维超过 32767 或 dense 体积超过预算时在导出入口禁用。

Bedrock Structure：

- Bedrock 1.20.10 little-endian NBT。
- 使用双 block index layer，方块状态由版本注册表转换。
- dense 体积超预算时提前拒绝。

Bedrock Function：

- 下载物为可安装行为包 ZIP，包含 1.20.10 manifest、入口 dispatcher 和分片函数。
- 使用流式 ZIP，不同时聚合全部命令文本和完整 ZIP。

完整工程 ZIP：

- 默认包含总体 Litematica、全部 `32^3` 子投影、拼接坐标、材料/箱子计划和机器 manifest。
- Schematic、mcstructure 与 mcfunction 为显式可选内容，默认关闭；独立的五类导出入口仍全部保留。
- 子投影两两无重叠，其并集必须完全等于总体投影。
- manifest 必须记录全局原点、每块 bbox、粘贴坐标、方块数、内容哈希和推荐施工顺序。
- 桌面端顺序写盘；Web 端有明确输出预算。
- ZIP 压缩必须限制并发，不得为数百个 entry 同时创建独立压缩 Worker；小文件可复用单一压缩管线或使用受控同步压缩。
- 导出进度细化到当前文件与格式，记录开始/结束时间、累计字节和阶段；自动化必须同时等待成功下载、明确失败 toast 或完成事件，不能盲等下载超时。
- 真实 320 全息默认工程 ZIP 已在约 `1.757s` 内完成，包含 18,760 方块与 140 个子区，且可解析回读；30 秒目标继续作为回归门槛。
- 显式勾选全跨平台内容时必须使用独立预算与可见进度，不能沿用默认包时限作虚假承诺。
- Web 工作集预检必须估算持久 packed block-state 数据、最大单个 `32^3` staging 区、NBT 编码/Gzip 副本和文档成本；不得把所有分片的临时 dense staging 同时计入峰值，也不得移除实际 packed 数据与编码副本余量。

### 6.8 384/2032 三道安全门

第一道，解锁：

- 默认输入最大 384。
- 使用自定义 `Windows.tsx` 弹窗，不使用浏览器 `confirm`。
- 取消后仍为 384；确认后最大值为 2032。

第二道，持续警告：

- 最终安全判定至少包括：目标高度超过 384，或已有投影实际 Y 跨度超过 384；任一成立即视为超限。
- 投影内部坐标是可平移的相对坐标。在没有用户选择世界放置原点时，不得仅因局部 `minY/maxY` 不落在 `-64..319` 就误判；以后若引入目标世界档案与放置原点，再增加绝对边界检查。
- 当前实现以 `max(目标高度, 实际投影 Y 跨度)` 判定超限；目标 320、实际跨度 385 的回归已通过。
- 超限时常驻黄色警告。
- 文案说明普通世界会截断，模组可能卡死或崩溃，并要求目标世界安装高度扩展。
- 折叠面板和移动端不能隐藏警告。

第三道，导出确认：

- 五类出口共用同一安全入口。
- 显示实际高度、384 阈值、数据包要求和风险。
- 用户必须勾选承担风险的复选框，确认按钮才可用。
- 关闭后勾选状态清零，回车不得绕过 disabled。

384 是 MELY 对原版世界高度的安全基线，不是所有结构文件格式自身的统一尺寸上限。技术上可导出的超高窄结构仍必须通过风险确认；超出编码器维度或内存预算的格式则在进入生成前禁用并解释原因。

边界验收矩阵：

| 场景 | 预期行为 |
| --- | --- |
| 高度 384 | 不显示最终超限确认；技术上可用的五类出口正常进入导出 |
| 高度 385 | 五类出口均受同一二次确认保护 |
| 窄小 2032 fixture | 五类出口均经过安全门，并分别按格式能力执行 |
| 真实比例 2032 fixture | 稀疏出口保持可用；超过 dense 预算的 Schematic/mcstructure 在计算前禁用 |
| 未勾选确认 | 确认按钮 disabled，Enter 不产生下载 |
| 取消或关闭 | 再次打开时复选框恢复未选中 |
| 完成导出 | `pendingExport` 与确认状态清零，仅继续当前选择的出口 |

上述矩阵当前均已有自动化证据；产品代码变更后必须完整回归，不能只复用旧报告。

2032 支持面向稀疏全息与可控 shell，不承诺任意模型的 2032 高 filled 工程。危险 filled 任务必须在三角形遍历前拒绝。

### 6.9 内存与稳定性

内存按 Windows 浏览器或 Tauri WebView 的完整进程树工作集峰值验收，不只看 JavaScript heap。

必须通过：

1. 爱莉希雅模型加载与预览。
2. 320 高全息生成与预览。
3. 320 高实体 shell 生成与预览。
4. 320 高完整工程 ZIP 导出。
5. 代表性 2032 高稀疏工程生成、分片和导出。
6. 连续十轮导入、生成、取消、切换与导出。

每条代表性流程峰值低于 2 GiB。十轮结束后 30 秒内，工作集应回落到首次稳定值的 120% 以内。

实现约束：

- ZIP 流式解包，避免压缩包与全部解压副本长期共存。
- Morph 使用稀疏拆分。
- 贴图按 UUID 去重；释放时 dispose geometry、material、texture，并关闭可关闭图像资源。
- CPU 快照使用最少 TypedArray，并将所有权移交 Worker。
- 同时只允许一个生成 Worker。
- shell 和全息使用稀疏结构。
- 流式压缩输入分段拥有独立 backing buffer，并对慢 sink 提供背压。
- ZIP entry 压缩采用有界并发，压缩器/Worker 完成后立即释放，禁止 entry 数量线性增长为活跃 Worker 数量。
- Web 大文件受预算保护；超大工程推荐 Tauri 直接写盘。

### 6.10 I18N

- 支持 `zh-CN`、`en-US`、`ja-JP`。
- 所有可见 UI、aria-label、title、toast、错误、警告和导出说明使用 TranslationKey。
- 三种 locale 的键、插值参数和模板必须一致。
- 不翻译格式字段、Minecraft ID、Pose JSON schema、坐标轴字母和品牌 MELY。
- 工程 ZIP 的 README、材料、箱子和拼接说明跟随导出 locale。
- 核心层抛稳定错误码，展示层负责本地化。
- 不允许把可达用户错误的英文 `Error.message` 作为 `error.external` 参数原样展示；真正仅供开发诊断的 invariant 可保留英文，但必须在边界处转为稳定错误码，并证明不会泄漏到用户界面。

## 7. 审批参考基线

以下信息来自截至 **2026-08-09** 已落盘的自动化报告，用于说明项目已经走到哪里。最新的资源释放草稿写入后尚未重跑完整验证，因此本节全部属于**历史基线**，不能替代第 8 节要求的最终源码证据。

### 7.1 构建与核心算法

- 某一较早源码基线的 TypeScript 类型检查与 Vite Web 生产构建通过。现有持久化全量日志只明确记录 `161` 项测试，而此前文稿曾写作 `176/176`；在最终可复现 runner 重跑前，不再引用未核实的 `176/176` 作为通过数字。
- 构建仍有第三方 MMD loader 的 Node 模块浏览器外置警告和主 chunk 体积警告；它们未导致当前 E2E 失败，但应在最终发布说明中保留。
- 真实爱莉希雅 ZIP 可加载，选中的主模型为 35,033 顶点、43,660 三角形、55 材质、1,294 骨骼。
- CPU 蒙皮真实审计通过：抽样 4,096 个 BDEF1/2/4 顶点，双参考最大误差 `9.555e-7`、RMS `3.918e-7`、P95 `8.169e-7`，全部低于 `1e-4`。
- 该真实模型只含 BDEF；SDEF/QDEF、Morph-before-skinning 和稀疏 Morph split 由独立确定性数学测试覆盖，不伪装成真实资产验证。
- PMX、PMD、ZIP、文件夹、相对贴图路径、缺贴图告警、候选模型切换和旧模型 dispose 均已有审计证据。

### 7.2 动作、姿势与视觉

- VMD/Pose 浏览器 E2E 已持久化：播放、点击与空格暂停、拖帧、逐帧、边界钳制、锁帧前禁生成、F0/F30 差异、Pose JSON 往返和投影哈希一致均通过。
- 真实 PMX 手动 K 姿势、撤销/重做、透视与正交面部聚焦通过；用户模型 ZIP 不含 VMD，VMD 部分使用仓库 fixture 与真实 PMX 联合验证。
- 320 实体三档均为 145,189 方块、尺寸 `249 x 318 x 208`、144 Regions；三档坐标完全一致。
- 艺术增强真实 E2E 通过：原色、关闭发光、大理石、蒸汽朋克、荒废遗迹、100% 抖动以及日夜预览均有截图，Console/Page Error 为 0。
- 面部结构审计通过：`strong >= balanced`，左右眼眉无跨中线连片；最终默认档仍需用户通过等机位对比图作审美批准。
- 320 全息真实生成成功：18,760 方块，其中 13,896 末地烛、4,864 独立白色玻璃板，尺寸 `255 x 320 x 209`。

### 7.3 生存工具、安全与格式

- 真实投影下材料、潜影盒换算、分类箱子、辅助箱隔离和槽位守恒全部通过。
- X/Y/Z 三轴切片守恒、图例、完成进度持久化与移动端触控切层通过。
- `1440x900`、`1024x680`、`390x844` 三视口无页面横向溢出，主要控件可达，Console/Page Error 为 0。
- 320 全息默认工程 ZIP 在约 `1.757s` 内导出，峰值 `1,245,765,632` bytes；18,760 方块、140 个子区可回读，默认不含可选跨平台格式。
- 同一份 145,189 方块真实实体投影已完成五格式守恒：Litematica、工程 ZIP、Schematic、mcstructure、mcfunction 的坐标/状态差异均为 0；Java DataVersion 为 3465，Bedrock 行为包最低版本为 1.20.10。
- `32^3` 子区已证明无重叠、并集完整，manifest 的 `buildOrder` 与 `contentHash` 有效。
- 高度矩阵已覆盖 384、385、窄小 2032、真实比例 2032，以及目标高度 320 但实际投影跨度 385；五类出口共完成 18 次下载，无确认门绕过。
- 模态键盘审计已覆盖初始焦点、Tab/Shift+Tab 圈定、Escape 关闭、异步打开后的焦点返回，以及所有图标式按钮和 Toggle 的本地化 aria-label/tooltip；真实 320 投影下全部通过。
- 格式解析回读不能代替目标游戏或模组中的实际导入与执行。
- 既有 I18N AST 扫描主要覆盖 `.tsx` 可见文案；它不能证明核心 `.ts` 异常已经本地化，因此 I18N 仍有发布缺口。

### 7.4 当前失败证据

- 十轮生命周期审计已完整执行，十轮生成、取消、模型切换和 Litematica 回读均通过，采样无错误，JS live heap约 40-42 MiB，DOM 节点与事件监听器稳定。
- 同一审计的完整进程树峰值为 `2,264,969,216` bytes，超过 2 GiB `117,485,568` bytes；30 秒恢复比为 `1.204318`，也略高于 `1.2`。峰值出现在第 9 轮切回真实 PMX，renderer 进程约 `1,792,794,624` bytes；具体资源滞留原因仍需实现阶段验证。
- 真实 2032 爱莉希雅全息工程 ZIP 已通过：113,936 方块、76,151 根末地烛、37,785 个玻璃板、4,234 个 `32^3` 区域；ZIP 为 4,974,558 bytes，导出约 93.077s，完整进程树峰值 `1,425,899,520` bytes。
- 修正后的工作集估值为 `332,160,640` bytes，继续使用 768 MiB Web 门槛；总体与分片逐坐标/逐状态并集、4,234 个内容哈希、施工顺序和 4,241 个逐文件完成事件均通过。

### 7.5 外部环境与审批边界

- 当前只读探测确认 Windows x64、Node `24.19.0`、npm `11.17.0`、Cargo/Rust `1.93.1`、Tauri CLI `2.11.4` 和 WebView2 `151.0.4129.72` 可用；`VsDevCmd.bat` 缺失。Tauri CLI 可通过绝对 Node 路径调用，因此 CLI 本身不是当前阻断。
- 本机尚无已验证可用的 Java 1.20.1 + Litematica/MaLiLib/WorldEdit、Bedrock 1.20.10 与代表性 2032 世界环境。
- Windows Tauri 构建仍缺已验证可用的 MSVC `link.exe` 与 Windows SDK；系统安装必须在用户另行授权后进行。
- Tauri 配置、图标、原生保存接口和 BAT 的 EXE 优先/Web 回退逻辑已存在，但安装包、双击启动、安装/卸载尚无真实 smoke。
- 项目不是 Git 仓库。任何后续工作都不得修改任何 `.gitignore`，除非用户另行明确授权。
- 当前批准只覆盖 R1 产品范围与执行顺序；不会自动批准最终面部效果、外部系统安装或 R1 发布。

### 7.6 最新未验证工作草稿

当前工作区在审批前已经存在以下内存修复草稿：

- `src/core/mmdModel.ts`：尝试深度释放 owned texture source、解绑 loader texture cache/options，并主动断开模型资源引用。
- `src/App.tsx`：尝试在加载新模型前先卸载并释放旧模型，再让出一帧。
- `src/components/Viewport3D.tsx`：加入 WebGL render-list 清理。
- `tests/mmdModel.test.ts`：加入 ImageBitmap、fallback toon 与 loader cache 的释放行为测试。

该草稿尚不可视为通过。批准后首先需要修正或证明以下问题：

- 返回对象保存的 pose 方法可能仍通过闭包持有旧 mesh。
- `renderer.renderLists.dispose()` 当前可能随 preview 或 height 变化过于频繁触发，应收窄到模型身份替换或明确的资源释放边界。
- 尚无该草稿对应的 TypeScript、定向测试、全量测试、Vite 构建或十轮生命周期结果。

## 8. R1 发布阻断项

以下任一项未完成，R1 不得标记完成。

### 8.1 产品与算法

1. 生成 `off / balanced / strong` 三档完全等相机的全身正面、正脸近景与斜角近景对比，由用户明确批准最终效果和默认档。
2. 若用户能提供第三方 VMD 或含非标准 IK、真实 SDEF/QDEF 的模型，则补充兼容 smoke；若 R1 冻结前仍无资产，必须把这些项目明确列为资产受限的兼容性残余风险，不能以 fixture 冒充实物结论。

### 8.2 性能与稳定性

3. 先审查并修正第 7.6 节的未验证草稿，再定位重复切换 PMD/真实 PMX 后 renderer 工作集滞留。优先审查 Three.js 纹理、解码图像源、Object URL、loader cache、pose 闭包、材质、场景、render list 与 WebGL 资源的所有权和释放顺序；必须以十轮实测证明，而非只依赖 `dispose()` 调用计数。
4. 十轮压力测试必须重新达到：完整进程树峰值 `< 2 GiB`、30 秒恢复比 `<= 1.2`、JS live heap 恢复、Worker/DOM/监听器稳定、轮次趋势稳定，且每轮取消与导出回读成功。门槛不得放宽。

### 8.3 UI 与安全

5. 在生命周期修复后，重跑键盘、三视口、384、385、目标 320/实际 385、窄小 2032、真实比例 2032 五类场景及五出口，Console/Page Error 必须为 0。
6. 盘点所有可达用户界面的核心与平台异常，将其转换为带参数的稳定 `AppError` 码；扩展扫描或静态规则覆盖 `.ts` 错误边界，并用中英日 E2E 证明导入、体素化、导出、桌面保存等失败路径不会泄漏英文内部消息。

### 8.4 格式与平台

7. 准备或由用户提供 Java 1.20.1 + Litematica/MaLiLib、WorldEdit、Bedrock 1.20.10 和代表性 2032 世界环境，完成目标端导入/执行 smoke；自动化 NBT 回读不能替代此项。
8. 在单独获得系统安装授权后安装 MSVC Build Tools/Windows SDK，完成 Tauri EXE 与至少一种 Windows 安装包构建。
9. 完成桌面启动、真实模型导入、生成、原生保存、安装/卸载，以及 BAT 的 EXE 优先、Web 回退、中文路径和含空格路径 smoke。
10. 所有产品修复完成后，重新通过 TypeScript、全量测试、Vite 构建、五格式真实守恒、320 工程包、2032 工程包、艺术/VMD/生存工具/高度矩阵、三语错误路径与生命周期审计；报告必须来自最终源码。

## 9. 批准后的实施顺序

### P0：正确性与性能阻断

1. 以 packed block-state、最大单区 staging 和编码副本模型修正 2032 工程包估算器，补定向单测。
2. 重新导出真实 2032 全息工程 ZIP，验证文件清单、4,234 个分片、坐标/状态守恒、施工顺序、内容哈希、耗时和完整进程树内存。
3. 审查现有内存释放草稿，消除 pose 闭包持有旧 mesh 与 render-list 过度清理风险，并补充定向生命周期测试。
4. 调查真实 PMX 重载时 renderer 工作集滞留，修正纹理、图像源、Object URL、loader cache、材质、场景和 WebGL 资源生命周期。
5. 重跑十轮导入、生成、取消、切换和导出审计，严格执行 `< 2 GiB` 与 `<= 1.2` 恢复门槛。
6. 对上述产品改动执行 TypeScript、全量测试和 Vite 构建，任何回归先在 P0 关闭。

### P1：产品 E2E

1. 生成三档面部增强的等相机全身/正脸/斜角对比图，由用户选择默认档；若需调整，仅修改表层重着色，不改变坐标或脸型。
2. 回归 PMX、PMD、ZIP、文件夹、缺贴图、多模型切换、VMD、Pose JSON、手动骨骼与已有 IK 路径。
3. 回归全息、实体、面部聚焦、夜景、发光、主题、抖动、材料、箱子和三轴分层指南。
4. 回归三视口、移动触控与完整键盘可访问性。
5. 回归 384/385/2032、实际跨度超限和全部导出安全入口。
6. 将可达核心/平台异常收束为稳定错误码，补中英日失败场景与 `.ts` 错误边界审计。
7. 对缺少真实资产的 SDEF/QDEF、非标准 IK 与第三方 VMD 明确记录残余风险，或在用户提供资产后补 smoke。

### P2：格式与 Windows 发布

1. 对最终源码再次执行五类真实导出的规范解析、状态和坐标守恒审计。
2. 由用户准备或批准准备 Java、WorldEdit、Bedrock 与高度扩展世界环境，执行目标端 smoke。
3. 单独请求并取得 MSVC Build Tools/Windows SDK 安装授权。
4. 构建 Tauri EXE 与 Windows 安装包，执行启动、原生保存、安装/卸载 smoke。
5. 双击 BAT，验证 EXE 优先、Web 回退、中文路径和含空格路径。

### P3：最终回归与交付

1. TypeScript 类型检查。
2. 全量自动化测试，使用可复现且稳定的执行方式。
3. Vite 与 Tauri 生产构建。
4. 三视口视觉、触控、键盘、内存与无错误回归。
5. 将每项需求链接到截图、JSON 报告或实际导出文件。
6. 由用户最终批准脸部效果和 R1 发布结果。

## 10. 完成定义

只有同时满足以下条件，当前 R1 Goal 才可结束：

- 第 8 节所有阻断项关闭。
- 代表性负载完整进程树峰值低于 2 GiB，危险负载在计算前明确拒绝。
- 锁定动作的源模型画面、CPU 快照和生成投影语义一致。
- 全息输出只包含竖直末地烛或独立竖直白色玻璃板。
- `32^3` 子投影可以无损拼回总体投影。
- Java 1.20.1 与 Bedrock 1.20.10 的版本、方块键和状态严格区分。
- 各导出 adapter 回读后的坐标和状态与同一 `ProjectionDocument` 守恒，跨版本不通过伪造 DataVersion 实现。
- 高于 384 的五类导出均不能绕过风险确认。
- 中英日 UI、玩家指南与用户可达错误路径无未批准硬编码文案或英文内部异常泄漏。
- Windows 用户可通过 BAT 或安装应用进入 MELY 并原生保存。
- 用户明确批准最终面部效果。

## 11. R1 明确不包含

- 完整 VMD 动画创作、关键帧曲线编辑器或逐帧动画投影序列。
- 精确复刻 Minecraft 光照传播。
- 自动兼容全部旧版或未来版 Minecraft。
- Java 旧版本方块降级表与 Bedrock 其他版本 palette；这些版本只有建立独立 `GameVersionProfile` 并通过实机验证后才可声明支持。
- 任意自定义世界高度档案编辑器；R1 只提供 vanilla 与 2032 预设。
- 对任意 2032 高 filled 工程作低于 2 GiB 的承诺。
- 子投影的头部/身体/腿部智能语义命名。
- 分层指南的图片/PDF 导出或跨设备云同步。
- 箱子策略选择器；R1 使用固定的按类别方案。
- 自动修复所有非标准骨骼名、损坏模型或缺失资源。
- 将模型、贴图或第三方动作上传到服务器。

## 12. 审批清单

请确认以下整合决策：

- [x] 同意第 5 节列出的 R1 功能范围。
- [x] 同意 Java 1.20.1 / DataVersion 3465 与 Bedrock 1.20.10。
- [x] 同意 R1 不承诺向下兼容；以后兼容必须使用版本档案和方块降级，而非只修改版本号。
- [x] 同意默认 320、原版阈值 384、固定 2032 预设和 `32^3` 分片。
- [x] 同意全息只使用竖直末地烛和独立竖直白色玻璃板。
- [x] 同意面部增强只重着色已有表层，不增加方块改变脸型。
- [x] 同意 UI 采用参考图的专业工作台信息架构，但不复制其品牌与具体视觉资产。
- [x] 同意默认按类别分箱，不在 R1 增加箱子策略选择器。
- [x] 同意工程 ZIP 默认只包含 Litematica 总体/分片和施工指南，其他平台格式由用户显式勾选。
- [x] 同意分层指南 R1 仅提供网页交互与本地进度。
- [x] 同意五类出口与目标游戏/模组 smoke 均属于 R1。
- [x] 同意 2 GiB 按完整进程树峰值验收。
- [x] 同意 Windows 安装包是发布硬门槛，但系统级 Build Tools 安装必须另行授权。
- [x] 同意第 11 节列出的延期范围。

审批结果：**已批准 MELY R1 整合基线**。

实施从 P0 的内存草稿审查和最终源码验证开始，不会直接安装外部依赖。到达 Windows 工具链、Java/模组、Bedrock 或 2032 世界准备阶段时，仍会分别请求授权；最终面部档位与 R1 发布也需要单独批准。
